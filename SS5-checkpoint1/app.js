/// mảng: C,R,U,D \\\\ push(), log(), arr[i] =?, arr.splice( , , )


//// Phương thức về mảng: indexOf(), find(), includes(), map(), filter()
//// Duyệt mảng: for thuần, for in, for of, ...

//// VD mẫu về map: 

let arr = [1, 2, 3, 4];
/// Tăng từng phần tử mảng lên 2 đơn vị
console.log(
  arr.map(item => item + 2)
); // kết quả  ....

/// Filter tương tự (với thay hành động => điều kiện )

